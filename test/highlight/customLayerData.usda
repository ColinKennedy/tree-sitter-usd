#usda 1.0
(
    customLayerData = {
    # <- variable
        timecode timeCode = 10.0
        # <- type.builtin
        #        ^ variable
        #                   ^ float
        timecode[] timeCodeArray = [10.0, 20.0]
        # <- type.builtin
        #          ^ variable
        dictionary subDict = {
        # <- type.builtin
        #          ^ variable
            timecode timeCode = 10.0
            # <- type.builtin
            #        ^ variable
            timecode[] timeCodeArray = [10.0, 20.0]
            # <- type.builtin
            #          ^ variable
        }
    }
)
