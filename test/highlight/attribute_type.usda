dictionary foo
# <- type.builtin
half[] foo = [2, 1, 2]
# <- type.builtin
string foo = "something"
# <- type.builtin
timecode time = 1.0
# <- type.builtin
token[] purpose = ["default", "render"]
# <- type.builtin
