def Xform "cube" (
    assetInfo = {
    # <- variable

        # Known keys
        # <- comment
        asset identifier = @./Cube.txt@
        # <- type
        #     ^ variable
        #                  ^ text.uri
        asset[] payloadAssetDependencies = [@fizz.usd@, @buzz.usd@]
        # <- type
        #       ^ variable
        #                                   ^ text.uri
        #                                               ^ text.uri
        string version = "v1"
        int foo = 8
    }
)
{
}
