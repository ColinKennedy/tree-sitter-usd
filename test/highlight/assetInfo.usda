def Xform "cube" (
    assetInfo = {
    # <- variable

        # Known keys
        # <- comment
        asset identifier = @./Cube.txt@
        # <- type.builtin
        #     ^ variable
        #                  ^ text.uri
        asset[] payloadAssetDependencies = [@fizz.usd@, @buzz.usd@]
        # <- type.builtin
        #       ^ variable
        #                                   ^ text.uri
        #                                               ^ text.uri
        string version = "v1"
        int foo = 8
    }
)
{
}
