def Xform "cube" (
    assetInfo = {
    # <- variable

        # Known keys
        # <- comment
        asset identifier = @./Cube.txt@
        # <- type
        #     ^ variable
        #                  ^ string.special.asset
        asset[] payloadAssetDependencies = [@fizz.usd@, @buzz.usd@]
        # <- type
        #       ^ variable
        #                                   ^ string.special.asset
        #                                               ^ string.special.asset
        string version = "v1"
        int foo = 8
    }
)
{
}
