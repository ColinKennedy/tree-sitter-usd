==============================================================================
subLayers - empty
==============================================================================

(
    subLayers = []
)

------------------------------------------------------------------------------

(module
  (metadata
    (metadata_assignment
      (identifier)
      (list))))

==============================================================================
subLayers - single
==============================================================================

(
    subLayers = [@foo.usda@]
)

------------------------------------------------------------------------------

(module
  (metadata
    (metadata_assignment
      (identifier)
      (list
        (asset_path)))))

==============================================================================
subLayers - multiple
==============================================================================

(
    subLayers = [@foo.usda@, @bar.usda@, @fizz.usda@]
)

------------------------------------------------------------------------------

(module
  (metadata
    (metadata_assignment
      (identifier)
      (list
        (asset_path)
        (asset_path)
        (asset_path)))))

==============================================================================
subLayers - multiple and trailing comma
==============================================================================

(
    subLayers = [@foo.usda@, @bar.usda@, @fizz.usda@, ]
)

------------------------------------------------------------------------------

(module
  (metadata
    (metadata_assignment
      (identifier)
      (list
        (asset_path)
        (asset_path)
        (asset_path)))))
